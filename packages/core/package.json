{
  "name": "@veyr/core",
  "version": "0.1.0",
  "description": "Veyr's shared local-data engine: session-log scanners, pricing, status synthesis, Graphify build, and ~/.veyr/ readers. Consumed as TypeScript source by the CLI and VS Code extension, which each bundle it — this package is never published or built on its own.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
